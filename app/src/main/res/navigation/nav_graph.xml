<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/HomeFragment">

    <fragment
        android:id="@+id/HomeFragment"
        android:name="org.indiv.dls.onerepmax.home.HomeFragment"
        android:label="@string/intro_fragment_label"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_HomeFragment_to_ExerciseFragment"
            app:destination="@id/ExerciseFragment" />
    </fragment>

    <fragment
        android:id="@+id/ExerciseFragment"
        android:name="org.indiv.dls.onerepmax.stats.ExercisesFragment"
        android:label="@string/exercise_fragment_label"
        tools:layout="@layout/fragment_exercise">

    </fragment>
</navigation>